# how to get started with react component lib npm modules


```
$ cd ~/code
$ git clone https://github.com/nikfrank/component-lib-boilerplate my-lib
$ cd my-lib
$ yarn
$ emacs .
# do some coding...
$ yarn build
$ npm publish
```